import React from "react";
import { View, Text } from "react-native";

// Global Styles
import { globalStyles } from "@globalStyles/global";

const Plan = () => {
  return (
    <View style={globalStyles.container}>
      <Text>Rencana Screen</Text>
    </View>
  );
};

export default Plan;
